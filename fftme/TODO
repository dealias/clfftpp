Transposed data is faster; do this all the time?

Copying to local memory: worth it?  As the outer loop?  Copying back
using an out-of-place shuffle?

Put zeta table in constant memory (alows for one read for all threads).

Mess around with local and global group size.

Clean up fft.hpp.
